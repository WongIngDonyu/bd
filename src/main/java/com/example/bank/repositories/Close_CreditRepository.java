package com.example.bank.repositories;

import com.example.bank.models.Close_Credit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Close_CreditRepository extends JpaRepository<Close_Credit, Long> {
}
