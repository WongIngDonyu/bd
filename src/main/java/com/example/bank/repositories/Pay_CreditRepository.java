package com.example.bank.repositories;

import com.example.bank.models.Pay_Credit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Pay_CreditRepository extends JpaRepository<Pay_Credit, Long> {
}
